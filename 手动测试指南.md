# Supabase Auth 系统手动测试指南

## 🚀 测试准备

### 1. 确认应用运行
```bash
# 检查应用状态
curl http://localhost:3000

# 应该返回 HTTP 200
```

### 2. 确认Supabase配置
检查 `.env.local` 文件包含：
```env
NEXT_PUBLIC_SUPABASE_URL=https://czggviorchhfzvseqilq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_eJd-THvuxBXGZB4BC1dJGQ_lZWNz4Lj
```

## 🧪 测试步骤

### 步骤1：访问登录页面
**URL**: http://localhost:3000/login

**预期结果**：
- ✅ 页面正常加载
- ✅ 显示"灵现写作"品牌
- ✅ 有登录/注册切换按钮
- ✅ 有邮箱和密码输入框
- ✅ 有Google登录按钮

### 步骤2：测试注册功能
**测试账户**：
- 邮箱: `test${时间戳}@example.com` (如: `test1771941053744@example.com`)
- 密码: `TestPassword123`

**操作**：
1. 确保在"注册"模式
2. 输入测试邮箱和密码
3. 点击"创建账户"按钮

**预期结果**：
- ✅ 显示"注册成功！请检查邮箱确认邮件。"
- ✅ 自动切换到登录模式
- ✅ 在Supabase控制台可以看到新用户

### 步骤3：测试登录功能
**操作**：
1. 使用刚才注册的账户
2. 输入邮箱和密码
3. 点击"登录账户"按钮

**预期结果**：
- ✅ 显示"登录成功！正在跳转..."
- ✅ 自动跳转到仪表板页面 (`/dashboard`)
- ✅ 显示用户邮箱和欢迎信息

### 步骤4：测试仪表板访问
**URL**: http://localhost:3000/dashboard

**预期结果**：
- ✅ 显示用户仪表板
- ✅ 显示用户邮箱
- ✅ 有"退出登录"按钮
- ✅ 有写作功能入口

### 步骤5：测试登出功能
**操作**：
1. 点击"退出登录"按钮

**预期结果**：
- ✅ 自动跳转到登录页面
- ✅ 会话被清除
- ✅ 再次访问仪表板会被重定向

### 步骤6：测试路由保护
**测试URL**：
- http://localhost:3000/dashboard (需要登录)
- http://localhost:3000/write (需要登录)
- http://localhost:3000/profile (需要登录)

**预期结果**：
- ❌ 未登录时访问：重定向到 `/login?redirect=原路径`
- ✅ 登录后访问：正常显示页面

### 步骤7：测试公共页面
**测试URL**：
- http://localhost:3000/ (首页)
- http://localhost:3000/login (登录页)

**预期结果**：
- ✅ 无论是否登录都可访问
- ✅ 已登录用户访问 `/login` 会被重定向到仪表板

## 🔧 Supabase控制台配置检查

### 必需配置：
1. **启用Email/Password认证**
   - 进入: Authentication → Providers
   - 启用: Email/Password

2. **配置网站URL**
   - 进入: Authentication → URL Configuration
   - Site URL: `http://localhost:3000`

3. **配置重定向URL**
   - 进入: Authentication → URL Configuration
   - Redirect URLs: `http://localhost:3000/auth/callback`

### 可选配置（Google OAuth）：
1. **创建Google OAuth应用**
   - 进入: Google Cloud Console
   - 创建OAuth 2.0客户端ID
   - 配置授权重定向URI: `https://czggviorchhfzvseqilq.supabase.co/auth/v1/callback`

2. **在Supabase启用Google认证**
   - 进入: Authentication → Providers
   - 启用: Google
   - 填入Client ID和Client Secret

## 🐛 常见问题排查

### 问题1：注册失败，显示"操作失败"
**可能原因**：
1. Supabase Email/Password认证未启用
2. 网络连接问题
3. API密钥无效

**解决方案**：
1. 检查Supabase控制台认证配置
2. 检查网络连接
3. 验证API密钥有效性

### 问题2：登录后无法跳转
**可能原因**：
1. 中间件配置问题
2. 会话cookie设置问题

**解决方案**：
1. 检查浏览器控制台错误
2. 清除浏览器cookie后重试
3. 检查中间件日志

### 问题3：Google登录按钮无效
**可能原因**：
1. Google OAuth未配置
2. 重定向URL配置错误

**解决方案**：
1. 配置Google OAuth或暂时禁用测试
2. 检查Supabase重定向URL配置

### 问题4：页面样式异常
**可能原因**：
1. Tailwind CSS未正确加载
2. 缺少Lucide React图标

**解决方案**：
1. 检查控制台CSS错误
2. 确认依赖包已安装

## 📊 测试记录表

| 测试项目 | 状态 | 备注 |
|---------|------|------|
| 登录页面访问 | ✅/❌ | |
| 用户注册功能 | ✅/❌ | |
| 用户登录功能 | ✅/❌ | |
| 仪表板访问 | ✅/❌ | |
| 登出功能 | ✅/❌ | |
| 路由保护 | ✅/❌ | |
| 公共页面访问 | ✅/❌ | |
| Google登录 | ⚠️/❌ | 需要配置 |

## 🎯 测试完成标准

### 必需功能（必须通过）：
- [ ] 用户注册（邮箱密码）
- [ ] 用户登录（邮箱密码）
- [ ] 用户登出
- [ ] 路由保护
- [ ] 会话管理

### 增强功能（建议通过）：
- [ ] Google OAuth登录
- [ ] 邮箱确认功能
- [ ] 密码重置功能
- [ ] 记住登录状态

## 📞 技术支持

### 查看日志：
```bash
# 应用日志
tail -f /tmp/lingguang-auth-test*.log

# 浏览器控制台
# 按 F12 打开开发者工具
```

### 测试工具：
```bash
# 运行自动化测试
node test-auth-simple.js

# 检查HTTP状态
curl -I http://localhost:3000/login
```

### 联系信息：
- **应用版本**: v0.20 + Auth系统
- **测试时间**: 2026-02-24
- **Supabase项目**: czggviorchhfzvseqilq.supabase.co

---

**测试状态**: 🟢 准备就绪  
**配置状态**: ✅ 基础配置完成  
**依赖状态**: ✅ 所有依赖已安装  
**文档状态**: ✅ 完整测试指南  

*开始你的认证系统测试吧！* 🚀